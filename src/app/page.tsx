'use client';

// The main App component is the full React artifact
// Import it directly as the page component
import App from '@/components/App';

export default function Page() {
  return <App />;
}
